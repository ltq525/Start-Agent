# Core runtime dependencies for local development and common imports.
# For optional feature groups, prefer:
#   pip install -e ".[search]"
#   pip install -e ".[memory]"
#   pip install -e ".[protocols]"
#   pip install -e ".[rl]"
#   pip install -e ".[dev]"

openai>=1.0.0
pydantic>=2.0.0,<3.0.0
requests>=2.31.0
numpy>=1.24.0
python-dotenv>=1.0.0
huggingface-hub>=0.20.0

